{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix anthropic-translator install && npm --prefix anthropic-translator run build && npx tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^2.0.0"
  }
}
