// The annotations are SOURCE-retention, the api contract supplies the seam and typed-key interfaces,
// and :ir supplies types this module names but never constructs. So all three are compileOnly: this
// module's jar carries none of them, and no :teavm module embeds a second copy of any.
dependencies {
    githubCompileOnly "intisy-ai:api:1.3.0:annotations"
    githubCompileOnly "intisy-ai:api:1.3.0:contract"
    githubCompileOnly "intisy-ai:core-ir:1.3.1:ir"
    githubAnnotationProcessor "intisy-ai:api:1.3.0:processor"
    githubAnnotationProcessor "intisy-ai:api:1.3.0:annotations"
}

tasks.named("compileJava") {
    options.compilerArgs += [
        "-Atsemit.name=auth-contracts",
        "-Atsemit.ext=.ts",
        "-Atsemit.keys=@intisy-ai/api/contract",
        "-Atsemit.imports=@intisy-ai/core-ir=IrRequest,IrResponse,IrEventStream,HandlerCtx",
    ]
}
